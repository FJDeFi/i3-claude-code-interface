fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
httpx>=0.27
pytest>=8.0
# sqlite3 is part of Python stdlib; no extra package required.
# tmux is a system dependency, install via the OS package manager.
